server:
  port: 8764

spring:
  application:
    name: service-zuul
zuul:
  routes:
    api-a:
      path: /api-console/hi
      serviceId: service-console
    api-b:
      path: /api-auth/hi
      serviceId: service-auth
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
